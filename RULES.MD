# RULES.MD

本文件根据 `README.MD` 提炼项目使用与开发规则，便于快速执行与排查。

## 1. 适用范围
- 本项目是一个“仿小米风格”照片大水印工具。
- 提供两种入口：
  - 命令行：`批量加大水印.py`
  - 图形界面：`仿小米水印界面版.py`

## 2. 运行环境规则
- Python 版本必须为 **3.8+**。
- 依赖要求：
  - `Pillow`
  - `piexif`
  - `PyQt5`（仅 GUI 版本）
- 推荐安装命令：
  - `pip install pillow piexif pyqt5`

## 3. 输入与输出规则
- 当前仅保证处理小写扩展名 `.jpg` 文件。
- 对目录输入：
  - 处理目录内所有 `.jpg`。
  - 输出到同级目录 `原目录名加水印`。
- 对单文件输入：
  - 输出到同目录。
  - 文件名格式：`原文件名-加水印.jpg`。

## 4. EXIF 与文案规则
- 默认读取并展示以下 EXIF 信息：
  - 机型
  - 拍摄时间
  - 光圈
  - 快门
  - ISO
  - GPS
- 可通过参数覆盖机型文案（`camera_name`）。
- 当无 GPS 时，使用自定义地点文案（`place`）。
- 若 EXIF 关键信息不完整，脚本可能跳过该文件并提示“获取的参数不全”。

## 5. 水印样式规则
- 支持白底或黑底：
  - `black=True`：黑底白字
  - `black=False`：白底黑字
- 支持 Logo 切换，素材位于 `watermark/`：
  - `mi.png`
  - `Leica.png`
  - `Apple.png`
  - `yc.png`
- 可替换同名素材或在代码中新增映射。

## 6. 核心函数参数规则
核心处理函数：`add_watermark(...)`
- `jpg_file`：`[目录, 文件名]`
- `camera_name`：自定义机型（覆盖 EXIF 机型）
- `black`：是否黑底
- `is_img`：是否单图输出模式
- `place`：无 GPS 时默认地点文案
- `logo`：`watermark/` 下的 Logo 文件名

## 7. 跨平台与兼容性规则
- 当前代码字体路径默认为 Windows 字体目录（如 `C:\Windows\Fonts\msyhbd.ttc`）。
- 在 Linux/macOS 上需改为本机可用字体路径，否则可能报错。
- 目前逻辑未覆盖 `.jpeg`、`.png` 或大写扩展名（如 `.JPG`）。
- 现有逻辑中 ISO 文案拼写为 `IOS`，属于已知表现。

## 8. 使用建议
- 批量处理前先用 1 张样例图验证效果与字体可用性。
- 在非 Windows 环境先确认字体路径，再进行批量任务。
- 对缺失 EXIF 的图片建议提前筛选或补全元数据。
